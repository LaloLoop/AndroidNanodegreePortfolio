<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Set buttons theme -->
        <!--<item name="android:buttonStyle">@style/CommonAppButton</item>-->
    </style>

    <!-- Buttons style -->
    <style name="CommonAppButton" parent="@android:style/Widget.Button">
        <item name="android:layout_width">200dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:background">@color/standardButtonColor</item>
        <item name="android:textColor">#FFF</item>
        <item name="android:layout_marginBottom">10dp</item>
    </style>

    <style name="CommonAppButton.Own">
        <item name="android:background">@color/highlightdButtonColor</item>
    </style>

</resources>
